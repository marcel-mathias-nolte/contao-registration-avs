# Contao 4 Extended Registration Bundle with AVS field

Adds a gdpr and an avs field to the member table and extends the registration process by sending an submitted photo along the admin notification.
